<h1>Bookshops you Recommended</h1>
<ol class="container-1">
    <%current_user.outgoing_recs.each do |recommendation|%>
        <li class="container-2">
            <p>You recommended <%= link_to recommendation[1].name, bookshop_path(recommendation[1])%> to <%=link_to recommendation[0].name, user_path(recommendation[0])%></p>
            <p><%=recommendation[2]%></p> 
        </li>
    <%end%>

</ol>