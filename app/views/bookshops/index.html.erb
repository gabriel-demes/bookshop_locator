<h1> Bookshop Index </h1>

<ol>
    <% @bookshops.each do |bookshop_instance| %>

        <li>
            <%= link_to bookshop_instance.name, bookshop_path(bookshop_instance) %>
        </li>

    <% end %>

</ol>
<%= render "/shared/search" %>
